# The pre-v1.0 audit — 2026-09-23

Two standing instructions come due before a v1.0 tag, and neither had been run.

> **§6 step 4.** Read `2026-09-20-user-view.md` — this same design described from the
> **user's chair**. It adds no decisions and the architecture file outranks it, but reading
> it is how the fifth pass found two blockers four builder-seat passes had missed. Re-run
> that read-back before P1 and **before v1.0**.

> **§6.1 rule 9.** The audit is part of the method, not an event. The 2026-09-20 pre-build
> audit found **7 blockers in a document whose every section was marked LOCKED** — three of
> them defeating D7. None came from new information; they came from reading the design as an
> adversary and from checking three validated projects' issue trackers for the same bug.
> **Expect the same yield before P1 and before v1.0, and run it the same way.**

The yield this time: **one security defect**, **two false user-facing claims**, and **six
stale statements** in the read-back file. The security defect was in code written the same
day, by the same session, with tests passing.

---

## 1. The security defect — a rule file could be a symlink

Found by building a hostile project rather than by reading the code.

```
.kiln/rules/evil.md -> /etc/hostname
| src/** | evil.md |
```

`classify` resolved the rule file with `resolve(rulesDir, file)` and checked
`isInside(rulesDir, path)`. Both are string arithmetic and neither knows what a symlink is,
so the path was judged to be inside `.kiln/rules/`, `existsSync` followed the link and
agreed it was there, and doctor reported:

```
[ ok ] rules routing: 1 rule file(s), 1 route(s), all resolved
```

`kiln rules w1 --stage plan` then printed the file:

```
--- evil.md · routed from src/** ---
HCM-DKP-096
```

That is the machine's hostname. Point the link at `.env`, `~/.aws/credentials` or a private
key and **the rules router is a file-read primitive that hands the contents to the agent**.

### Why it got in

D52 settled this for **writes**: a symlink leaf is rejected, never followed — the
arbitrary-file-overwrite primitive agent-skills #295 rates High. The write path goes through
`resolveTarget`, which refuses a link. The reader was new code and used a plain `resolve`.

The lesson generalises past this one line: **a safety rule that lives inside one helper is
only as wide as that helper's callers.** `resolveTarget` carried D52; anything resolving a
path another way inherited nothing.

Fixed in #117. A rule file that is a symlink, or sits behind a symlinked directory, resolves
to `escapes` and is never read — the directory half matters, because refusing only the leaf
leaves `rules/theirs/x.md` where `theirs` is the link.

---

## 2. "That is all" — the update path

`user-view.md` §14 read:

> Updating kiln: `/plugin update kiln`. **That is all** — kiln writes nothing there (D39).

Both halves are true and the conclusion is false. Writing nothing into the project is the
point; it is also why a checkout that already had the push floor keeps the hook the **older**
kiln wrote. D105 made that a `stale` finding, reported to whoever runs `kiln doctor`.

Measured on a real monorepo after rc.19: **five checkouts all `stale`**, having run the whole
of rc.18 carrying the submodule bug rc.19 had fixed, while every run printed `Ready.`

The whole of kiln's advice about updating pointed the user at a finished state they had not
reached. Fixed in #118 (D104): `kiln open` reports an unarmed floor once per run, and both
the README and §14 now say `/plugin update kiln`, **then `kiln doctor`**.

---

## 3. The six stale statements

A read-back's ordinary yield. None is a defect; all of them describe a product that has moved.

| § | Said | Is |
|---|---|---|
| header | "Derived from D1–D85. Last updated: 2026-09-20" | D1–D104 |
| 1 | asks "protected branches, test command, **tracker** (or none)" | the third question is `stack.cmd.test_fast`; tracker is conditional on there being no remote |
| 3 | the `bounded` flow, with no rules step | `kiln rules` runs at PLAN and at REVIEW (D101) |
| 8 | the on-disk tree, without `.kiln/hooks/` | the floor's runner is committed there on purpose, so a stranger can read what runs in their repository (D90.3) |
| 15 | interpreter edits are a ceiling, full stop | D98 blocks an inline program that *names* a file the enforcement is made of. The ceiling is **source** edits only |
| 18 | "`guard-gate` has never run anywhere" | it has: the live harness, six acceptance runs, ten real checkouts. Two false blocks were found that way and fixed (D93, D103) |

§18's line is the one worth keeping in mind. It was written as a warning about untested code,
and leaving it there after the code had been exercised would misreport a real measurement as
an open risk — which is D7 item 3 pointed at kiln's own documentation.

---

## 4. What the audit did **not** find

Stated so the pass is not read as more than it was.

- **No D7 item held falsely.** All seven have tests; the seven were re-run on ten real
  checkouts as part of the same day's matrix, and the push floor refused a real push in every
  layout that had one.
- **No disagreement between the architecture file and the product** beyond the three already
  corrected in #114 (skills 8 → 7, core guards 3 → 5, §6 naming P0 as the next action).
- **Nothing in upstream's releases to adopt.** superpowers `main` is 0 commits ahead of the
  fork point; the issue tracker is where the value was, and it produced #116.

## 5. The method note

Two of the three findings came from doing something rather than reading something: building a
hostile `.kiln/rules/` and re-running the corpus after an update. The stale statements came
from reading — and they are the least valuable third.

That is the same ratio the 2026-09-20 audit reported, and it is the argument for the read-back
being a **build** step rather than a proofread. A section of prose can only be checked against
another section of prose. A product can be asked.

---

## 6. The second pass — the design read against the product, and against OSS

Same day, a second read with a different method. The design was compared with the code, every
finding was re-run by an adversarial pass that tried to disprove it, and each surviving one
was weighed against how mature open-source projects solve the same problem: superpowers,
BMAD and agent-skills for the workflow; cc-safety-net and destructive_command_guard for the
guards; Claude Code's own permission documentation. The owner's rule for the pass: where a
mature project has the better approach, adopt it, and a fix is general across projects or it
is not a fix.

### What it found

| Class | Found | Decisions |
|---|---|---|
| A guard verdict wrong, through the real dispatcher | 33 of 142 corpus rows | D125–D132, D135 |
| A failure branch ending in ALLOW | 4 (config, work directory, stack guard, floor) | D133 |
| A run-flow state with the guards off | 3 (pass 2, after review, `ship --opened`) | D136–D138 |
| Evidence or approval bound to nothing | 3 (gate hash, review anchor, stale green) | D139–D141 |
| Skill text contradicting the gates | 6, and one instruction no verb could carry out | D142, D146 |
| Smaller promises with no code | 6 | D143–D145 |
| Stale documentation | ~25 lines across six files | fixed in place |

Two of the corpus's rows were bypasses nobody had reported: a command after a heredoc
terminator was invisible, and `git checkout main && git commit` was judged against the branch
the session stood on.

### What the adversarial pass took back

Stated so the first report is not read as more than it was.

| First reported | After re-running and weighing |
|---|---|
| `--auto` on `full` needs no separate opt-in — a bug | deliberate and commented in code; D16's wording was stale (D147) |
| the heredoc in `python3 - <<EOF` — a hole | the named ceiling was the *script file*; the inline form is closed by D129 |
| `cd X && git push` — a push to a protected branch | real, but only with submodules and an unknowable directory, and the floor still refuses |
| a corrupt config switches stack guards off — general | only a stack that ships guards, php-ci3 |
| gate without `--artifact`, review of zero files — agent-facing | not reachable on the skill's documented path; fixed because a deviation should not open a hole |
| the brainstorming skill was badly edited | faithful to upstream; kiln had not adapted it to its own gates |
| commit-per-task contradicts the design | superpowers does it, BMAD and agent-skills do not; the owner chose BMAD's (D142) |

### Benchmarks

No standard benchmark measures a guard: the academic suites (RedCode, SABER, AgentDojo,
OS-Harm and others) measure whether a *model* does harm. The corpus in D125 is the instrument,
and "never" is stated as the bound a sample supports. A capability benchmark (SWE-bench Pro,
through Harbor) was judged feasible and not required; the one credible precedent — a paired run
of superpowers on 500 tasks — found +2.2 points of resolve rate, not significant, and 40% more
tokens.
