[{"id":"config-never-read","where":"src/http.js get()","what":"src/http.js still uses its hard-coded TIMEOUT_MS constant and never reads config.http.timeoutMs, so HTTP_TIMEOUT_MS has no effect"},{"id":"vacuous-test","where":"test/config.test.js","what":"the test asserts `timeoutMs ?? DEFAULT` equals the default, which passes even when the setting is ignored or missing; nothing tests that the client uses the configured value"}]
