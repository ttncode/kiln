# tasktrack

Tracks projects for a small agency: a dashboard of project status, customer order history,
and staff accounts. Plain Node, no dependencies. `npm test` runs the suite; `npm start`
serves on port 3000.
