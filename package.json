{
  "name": "kiln",
  "version": "0.0.0",
  "description": "A harness plugin that drives one unit of work to a reviewed, verified, ship-ready change, and blocks unsafe actions at runtime.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "test": "node --test tests/",
    "lint": "eslint .",
    "check": "npm run lint && npm test"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^9.36.0"
  }
}
